#!/bin/bash

# File: 07_compute.yaml
# Brief: Test nr. 7 - Advanced HOT Resolution
#        Contains 5 HOT files and 2 environment files

parameters:
  GlanceHost:
    type: string
  KeystonePublicApiVirtualIP:
    type: string
  NovaApiHost:
    type: string
  NovaPublicIP:
    type: string
  NetIpMap:
    type: json

resources:
  XYZ:
    type: OS::TripleO::Nova::Server
    properties:
      # getting value of net_ip_map in netipmap.yaml > controller > root > compute
      NetMap: {get_param: NetIpMap}
